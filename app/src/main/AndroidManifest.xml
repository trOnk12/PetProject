<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.example.myapplication">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
            android:name=".MyApplication"
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/AppThemeNoActionBar">
        <activity
                android:name=".presentation.route.RouteActivity"
                android:noHistory="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity android:name=".presentation.MainActivity">

        </activity>

        <activity android:name=".presentation.comments.CommentsActivity">

        </activity>

        <activity android:name=".presentation.comments.CommentsDetailActivity">

        </activity>

        <activity android:name=".presentation.search.SearchActivity"
                  android:parentActivityName=".presentation.MainActivity">

        </activity>
    </application>

</manifest>